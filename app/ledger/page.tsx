// app/ledger/page.tsx

import LedgerClient from "@/components/restro/LedgerClient";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function LedgerPage() {
  return <LedgerClient />;
}// app/ledger/page.tsx

import LedgerClient from "@/components/restro/LedgerClient";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function LedgerPage() {
  return <LedgerClient />;
}
